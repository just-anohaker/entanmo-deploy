"use strict";

// const program = require("commander");

// const { doctor, account, network, config } = require("../lib/modules/config");
// const { colorError } = require("../lib/utils/colorful");
// const Commands = require("../lib/commands");

module.exports = () => {
    // program
    //     .command(Commands.account)
    //     .description("")
    //     .action(() => {
    //         doctor(Commands.account)
    //             .then(() => {
    //                 return account();
    //             })
    //             .catch(error => {
    //                 console.log(`[Account] ${colorError(error.toString())}`);
    //             });
    //     });

    // program
    //     .command(Commands.network)
    //     .description("")
    //     .action(() => {
    //         doctor(Commands.network)
    //             .then(() => {
    //                 return network();
    //             })
    //             .catch(error => {
    //                 console.log(`[Network] ${colorError(error.toString())}`);
    //             });
    //     });

    // program
    //     .command(Commands.config)
    //     .description("")
    //     .action(() => {
    //         doctor(Commands.config)
    //             .then(() => {
    //                 return config();
    //             })
    //             .catch(error => {
    //                 console.log(`[Config] ${colorError(error.toString())}`);
    //             });
    //     });
};